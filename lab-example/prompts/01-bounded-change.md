# Prompt fixture 01

Add a `farewell(name)` function to `app.py` that returns `Goodbye, {name}!`, add one test for it, and report the exact validation command and result. Do not modify files outside this project.

This is a static prompt fixture for the research artifact. It was not sent to an LLM in this repository.
